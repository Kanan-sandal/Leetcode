class Solution {
  public:
    bool armstrongNumber(int n) {
        int sum=0;
        int num=n;
        while(num>0){
            int digit=num%10;
            sum+=pow(digit,3);
            num=num/10;
        }
        if(sum==n) return true;
        else return false;
    }
};
